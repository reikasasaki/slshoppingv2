<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.slshop.cartitem.CartItemMapper">
	
	<resultMap id="cartItemMap"
		type="com.slshop.common.entity.CartItem">
		<id column="ID" property="id" />
		<result column="QUANTITY" property="quantity" />
		<result column="" property="quantity" />
		<association property="customer"
			javaType="com.slshop.common.entity.Customer">
			<id column="CUSTOMER_ID" property="id" />
		</association>
		<association property="product"
			javaType="com.slshop.common.entity.product.Product">
			<id column="PRODUCT_ID" property="id" />
		</association>
	</resultMap>


	<insert id="insert" useGeneratedKeys="true" keyColumn="ID">
		INSERT
		INTO CART_ITEMS (ID, CUSTOMER_ID, PRODUCT_ID,
		QUANTITY) VALUES
		(CART_ITEMS_ID_SEQ.nextval, #{customer.id},
		#{product.id}, #{quantity})
	</insert>
	
	<select id="findItem" resultMap="cartItemMap">
		SELECT
		ID,QUANTITY
		FROM
		CART_ITEMS
		WHERE
		CUSTOMER_ID = #{customerId}
		AND
		PRODUCT_ID = #{productId}
	</select>

	<update id="update">
		UPDATE
		CART_ITEMS
		SET
		QUANTITY = #{quantity}
		WHERE
		CUSTOMER_ID = #{customer.id}
		AND
		PRODUCT_ID = #{product.id}
	</update>
</mapper>