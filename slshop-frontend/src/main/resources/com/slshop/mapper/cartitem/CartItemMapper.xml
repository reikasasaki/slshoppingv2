<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.slshop.cartitem.CartItemMapper">
	
	<resultMap id="cartItemMap"
		type="com.slshop.common.entity.CartItem">
		<id column="ID" property="id" />
		<result column="QUANTITY" property="quantity" />
		<association property="customer"
			javaType="com.slshop.common.entity.Customer">
			<id column="CU_ID" property="id" />
		</association>
		<association property="product"
			javaType="com.slshop.common.entity.Product">
			<id column="P_ID" property="id" />
		</association>
	</resultMap>


	<insert id="insert" useGeneratedKeys="true" keyColumn="ID">
		INSERT
		INTO CART_ITEMS (ID, CUSTOMER_ID AS CU_ID, PRODUCT_ID AS P_ID,
		QUANTITY) VALUES
		(CART_ITEMS_ID_SEQ.nextval, #{customerId},
		#{productId}, #{quantity})
	</insert>

</mapper>